<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/app.css" />
    <title>User Profile</title>
  </head>
  <body>
    <h1>Welcome <%= users.name %></h1>
    <br /><br />
    <form action="/users/profile/<%= users.id %>?_method=PUT" method="POST">
      Name:
      <input type="text" name="name" value="<%= users.name %>" /><br /><br />
      Username:
      <input
        type="text"
        name="username"
        value="<%= users.username %>"
      /><br /><br />
      Password:
      <input
        type="text"
        name="password"
        value="<%= users.password %>"
      /><br /><br />
      <select name="team">
        <% for ( let i = 0; i < teams.length; i++ ) { let selected = ( i == 0 )
        ? "selected" : ""; %>
        <option value="<%=teams[i].id%>" selected><%=teams[i].name%></option>
        <% } %>
      </select>
      <input type="submit" class="buttonize" value="Save Updates" />
    </form>
    <form action="/users/<%= users.id %>?_method=DELETE" method="POST">
      <input type="submit" class="buttonize" value="Delete Master" />
    </form>
    <br /><br />
    <a href="/pokemon">Back to Pokédex</a>
  </body>
</html>
